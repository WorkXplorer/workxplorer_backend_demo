# Generated by Django 5.1.15 on 2026-05-21

from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ("general", "0012_hh_market_snapshot"),
    ]

    operations = [
        migrations.RunSQL(
            sql="""
                ALTER TABLE general_candidateskillrecommendation
                    ADD COLUMN IF NOT EXISTS source_key varchar(255) NOT NULL DEFAULT '',
                    ADD COLUMN IF NOT EXISTS language varchar(5) NOT NULL DEFAULT 'uz',
                    ADD COLUMN IF NOT EXISTS vacancy_count integer NOT NULL DEFAULT 0,
                    ADD COLUMN IF NOT EXISTS minimum_years integer NOT NULL DEFAULT 0,
                    ADD COLUMN IF NOT EXISTS proficiency_level varchar(20) NOT NULL DEFAULT 'UNDEFINED',
                    ADD COLUMN IF NOT EXISTS marked_at timestamp with time zone NULL,
                    ADD COLUMN IF NOT EXISTS added_at timestamp with time zone NULL,
                    ADD COLUMN IF NOT EXISTS vacancy_id uuid NULL;

                CREATE INDEX IF NOT EXISTS general_can_source_5eeb5b_idx
                    ON general_candidateskillrecommendation (source, normalized_skill_name);

                CREATE INDEX IF NOT EXISTS general_can_candida_d6d215_idx
                    ON general_candidateskillrecommendation (candidate_id, resume_id, status);

                CREATE INDEX IF NOT EXISTS general_candidateskillrecommendation_vacancy_id_idx
                    ON general_candidateskillrecommendation (vacancy_id);
            """,
            reverse_sql=migrations.RunSQL.noop,
        ),
    ]
